import { createPlaceholderScreen } from '@/components/ui';

export const GrammarScreen = createPlaceholderScreen({
  title: 'Grammar',
  emoji: '📖',
  description: 'Master English grammar rules and structures.',
});
